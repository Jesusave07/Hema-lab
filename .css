/* Base */
:root{
  --bg: #fbfcfe;
  --text: #1f2937;
  --muted: #6b7280;
  --accent: #0066cc;
  --accent-2: #ac2e5a;
  --container: 1100px;
  --gap: 1.25rem;
}

*{box-sizing:border-box}
html,body{height:100%}
body{
  margin:0;
  font-family: Inter, system-ui, "Segoe UI", Roboto, "Helvetica Neue", Arial;
  background:var(--bg);
  color:var(--text);
  -webkit-font-smoothing:antialiased;
  -moz-osx-font-smoothing:grayscale;
  line-height:1.5;
}

/* Container helper */
.container{
  width:90%;
  max-width:var(--container);
  margin:0 auto;
}

/* Header / nav */
.site-header{
  position:sticky;
  top:0;
  z-index:1200;
  background:linear-gradient(180deg, rgba(255,255,255,0.96), rgba(255,255,255,0.92));
  backdrop-filter: blur(6px);
  border-bottom:1px solid #e6e9ee;
}
.header-inner{
  display:flex;
  align-items:center;
  justify-content:space-between;
  gap:var(--gap);
  padding:0.75rem 0;
}
.brand{
  display:flex;
  align-items:center;
  gap:0.75rem;
  text-decoration:none;
  color:var(--text);
}
.brand img{
  width:56px;
  height:56px;
  object-fit:cover;
  border-radius:8px;
  display:block;
}
.main-nav ul{
  list-style:none;
  display:flex;
  gap:1rem;
  margin:0;
  padding:0;
  align-items:center;
}
.main-nav .nav-link{
  display:inline-block;
  padding:0.5rem 0.75rem;
  color:var(--text);
  text-decoration:none;
  font-weight:600;
  border-radius:6px;
  transition: all 180ms ease;
}
.main-nav .nav-link:hover,
.main-nav .nav-link:focus{
  background:rgba(0,102,204,0.08);
  color:var(--accent);
  outline:none;
}
.main-nav .whatsapp{
  color:#fff;
  background:linear-gradient(90deg,#25D366,#128C7E);
  padding:0.45rem 0.85rem;
}

/* Hero */
.hero{
  padding:3.25rem 0 2rem;
  text-align:left;
}
.hero .container{
  display:grid;
  grid-template-columns: 1fr 420px;
  gap:2rem;
  align-items:center;
}
.hero h1{
  font-size:clamp(1.6rem, 3.6vw, 2.4rem);
  margin:0 0 0.5rem;
  color:#0b1220;
}
.lead{
  color:var(--muted);
  margin-bottom:1rem;
  max-width:68ch;
}
.cta{display:flex;gap:0.75rem;margin-bottom:1.25rem}
.btn{
  display:inline-block;
  text-decoration:none;
  padding:0.6rem 0.9rem;
  border-radius:8px;
  font-weight:700;
  font-size:0.95rem;
}
.btn.primary{ background:var(--accent); color:#fff; }
.btn.outline{ background:transparent; color:var(--accent); border:1px solid rgba(0,102,204,0.12); }

/* Highlights */
.highlights{
  display:flex;
  gap:1rem;
  list-style:none;
  margin:0;
  padding:0;
}
.highlights li{
  background:#fff;
  padding:0.6rem 0.9rem;
  border-radius:8px;
  box-shadow:0 6px 20px rgba(16,24,40,0.04);
  font-weight:600;
}

/* Hero image */
.hero-image img{ width:100%; height:auto; display:block; border-radius:10px; box-shadow:0 8px 30px rgba(15,23,42,0.06) }
.hero-image figcaption{ font-size:0.85rem; color:var(--muted); margin-top:0.5rem }

/* Sections */
.section{ padding:2.5rem 0; }
.section h2{ font-size:1.5rem; margin-bottom:0.5rem; color:#0b1220 }
.services .service-list{
  display:grid;
  grid-template-columns: repeat(2,1fr);
  gap:1.25rem;
  margin-top:1rem;
}
.service{
  background:#fff;
  border-radius:10px;
  padding:1rem;
  box-shadow:0 6px 20px rgba(16,24,40,0.04);
}
.service img{ width:100%; height:140px; object-fit:cover; border-radius:8px; margin-bottom:0.6rem }
.service h3{ margin:0 0 0.4rem; font-size:1.05rem }
.service ul{ color:var(--muted); margin-top:0.6rem }

/* About features */
.about .features{
  display:grid;
  grid-template-columns:repeat(4,1fr);
  gap:1rem;
  margin-top:1rem;
}
.feature{ background:#fff; padding:1rem; border-radius:10px; text-align:center; box-shadow:0 6px 18px rgba(16,24,40,0.04) }

/* Contact grid */
.contact-grid{
  display:grid;
  grid-template-columns: 1fr 1fr;
  gap:1rem;
}
.contact-card, .locations{ background:#fff; padding:1rem; border-radius:10px; box-shadow:0 6px 18px rgba(16,24,40,0.04) }

/* Footer */
.site-footer{
  border-top:1px solid #eef2f6;
  padding:1rem 0;
  text-align:center;
  color:var(--muted);
}

/* Accessibility helpers */
.sr-only{
  position:absolute!important;
  width:1px;height:1px;padding:0;margin:-1px;overflow:hidden;clip:rect(0,0,0,0);white-space:nowrap;border:0;
}

/* Responsive */
@media (max-width: 980px){
  .hero .container{ grid-template-columns: 1fr; }
  .services .service-list{ grid-template-columns: 1fr; }
  .about .features{ grid-template-columns: repeat(2,1fr); }
  .contact-grid{ grid-template-columns: 1fr; }
}

@media (max-width:640px){
  .header-inner{ padding:0.5rem 0; }
  .main-nav ul{ gap:0.5rem; flex-wrap:wrap; justify-content:center }
  .brand img{ width:48px; height:48px; }
  .highlights{ flex-direction:column; align-items:flex-start }
  .hero{ padding-top:2rem }
}
